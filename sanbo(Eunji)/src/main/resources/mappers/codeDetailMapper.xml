<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
	
	
<mapper namespace = "com.sw.web.mapper.codeDetailMapper">

	<insert id = "insert">
		insert into common_code_detail (reprec, detailc, detailc_n, use) values (#{reprec}, #{detail}, #{detailc_n}, #{use})
		
	</insert>
	
	<select id = "selectAll" resultType="CodeDetailVO">
		select * from common_code_detail
	</select>
	
	<select id = "selectById" resultType = "CodeDetailVO">
		select * from common_code_detail where id = #{id}
	</select>
	
	<update id = "update">
		update common_code_detail set detailc=#{detailc}, detailc_n=#{detailc_n}
		where id = #{id}
	</update>
</mapper> 	